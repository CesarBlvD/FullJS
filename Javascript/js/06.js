// Objeto Math

let resultado;

resultado = Math.PI; //Muestra el numero pi
resultado = Math.round(2.6); //redondea al numero mas cercano
resultado = Math.cell(2.6); //Siempre redondea hacia arriba
resultado = Math.floor(2.6); //Siempre redondea hacia abajo
resultado = Math.sqrt(34); //Imprime la raiz cuadrada
resultado = Math.abs(-34); //Convierte nmeros negativos a positivos
resultado = Math.min(3, 5, 6 ,8); //dice el numero minimo de un listado
resultado = Math.max(3, 5, 6 ,8); //dice el numero maximo de un listado
resultado = Math.random(); //genera numero aleatorio
resultado = Math.floor(Math.random() * 30); //Primero lo que hace es generar un numero random y lo multiplica por 30 y con floor  lo redondea hacia abajo

console.log(resultado);